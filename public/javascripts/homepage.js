// Add interactivity to buttons
document.getElementById('loginBtn').addEventListener('click', () => {
    alert('Login button clicked!');
    // Redirect to login page or perform login action
  });
  
  document.getElementById('registerBtn').addEventListener('click', () => {
    alert('Register button clicked!');
    // Redirect to registration page or perform registration action
  });
  
  document.getElementById('whiteboardBtn').addEventListener('click', () => {
    alert('White Board button clicked!');
    // Redirect to whiteboard page or open whiteboard
  });